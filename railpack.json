{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "npm ci"
      ]
    },
    "build": {
      "commands": [
        "npm run prisma:generate",
        "npm run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm start"
  }
}

